下一版本：
	
	数据源防护支持正则表达式，支持URL和连接属性的或关系；
	匿名用户始终不开放数据源的SQL工作台、数据导入/导出功能；
	看板展示页chartPluginManager.js分组加载，避免单个文件尺寸过大；
	新增客户端（local）图表功能，不参与服务端交互，完全由客户端看板API操作？；
	图表插件属性/分组新增可重复repeatable特性，用于支持属性值数组输入需求；
	新增看板版本设计概念，1.0 保留现有看板API；2.0 移除已弃用看板API，将看板/图表API中的字段都改为函数：
		图表插件添加apiVersion属性，标识支持的看板版本；
		图表展示页根据插件的apiVersion自动加载对应看板API环境；
		看板编辑页新增看板版本设置项，展示页根据此版本号加载对应看板API环境；
		图表监听器、chart.update()等的results参数结构改为chartResult（对象结构，易于扩展）；
		chartFactory.init()改为prototype实现；
		dashboard.charts -> dashboard.charts()；
		dashboard.renderContext -> dashboard.renderContext()；
		chart.dashboard -> chart.dashboard()；
		chart.dataSetFieldSign() -> chart.dataSetFieldSigns()（因为是参数和返回值主要是数组）；
		chart.elementId -> chart.elementId()；
		chart.id -> chart.id()；
		chart.name -> chart.name()；
		chart.plugin -> chart.plugin()；
		chart.renderContext -> chart.renderContext()；
		chart.updateInterval -> chart.updateInterval()；
		chart.updateResults() -> chart.updateResult()；
		chart.extValue() -> chart.liveData()；
		chart.loadMap()改为fetch实现；
		chart.render()、update()函数仅接受图表结果对象参数；
		chart.off()函数必须指定要解绑的handler函数；
		移除chart.loadMap()函数;
		图表元素上的dg-chart-listener不再继承<body>元素上的；
		移除chart.resetDataSetParamValuesFirst()、chart.resetDataSetParamValues()函数；
		内置图表插件数据结构由value数组改为更常用的name/value结构；
		chart.dataSetFieldOfSign()函数取消nonNull参数逻辑，chart.dataSetFieldsOfSign()函数取消nonEmpty参数逻辑（定义图表时已经校验，这里无需再处理，增加函数复杂性）；
		移除ECharts内置看板库，改为通过图表插件depend依赖库引入（只需引入名称，库源由系统环境提供）；
		移除jQuery，库加载改为采用Promise实现（尝试scriptEle.async=false顺序加载）；
		移除chart.elementJquery()函数；
		看板内置资源库移除ECharts，改为在图表插件库中引入（内置共用库），避免仅表格看板仍加载ECharts库；
		图表JS对象移除ECharts相关API，改为以内置插件依赖库的方式支持；
		图表JS对象loadMap()改为loadResource()、dg-chart-map-urls改为dg-chart-resource-urls？；
		内置标签卡移除字体尺寸CSS样式；
		移除图表事件格式规范，改为由插件自决定，移除chart.eventHandlers()、chart.originalDataIndex()相关函数和规范；
		图表渲染器renderer.js扩展可选的图表联动支持函数：
			{
				eventTypes: [ "series.click", { value: "...", description: "" }, ... ]、function(){ return [...]; },,
				eventDataNames: function(eventTypeObj)
				{
					return [ "...", { value: "...", description: "" }, ... ];
				}
				eventDataValue: function(event, name)
				{
					return ...;
				}
			}
		移除chart.inflateRenderOptions()、chart.inflateUpdateOptions()函数，添加chart.processRenderOptions()、chart.processUpdateOptions()函数；
		移除jquery.datetimepicker日期组件，改为使用无依赖的组件（flatpickr）；
待定：
	
	看板可视编辑模式绝对/相对定位元素可拖拽；
	内置图表插件新增数据卡片，将每行数据渲染为一个数据卡片；
	图表插件数据标记自动绑定；
	图表插件定义常用图表属性；
	图表插件在线定义功能；
	用户/角色登录后跳转首页管理功能，可定义指定用户/角色登录后的跳转首页；
	看板图表导出数据功能；
	看板模板管理功能；
	看板编辑时自动保存临时内容；
	组合数据集：
		新建组合数据集（CombineDataSet），选定多个其他数据集，通过SQL语句连接、选取它们，生成新的数据；
	统计分析数据集；
	定时邮件报告；
	项目（包括数据集、图表、看板）导入/导出功能；
	服务端文件数据集支持${DG_LATEST_FILE}内置参数，用于读取最新文件；（暂不实现，因为无法支持子目录，用处不大）
	
待研究：
	提供服务API，使图表、看板可以方便整合至第三方前端代码中；
	多维分析图表类型；
	数据填报；
	应用级集成；
	3D图表；
	SQL跨库查询；
	docker安装镜像；
