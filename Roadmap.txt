下一主版本：
	
	扩展数据集功能，支持Excel、REST服务类的数据集；
	
下一修订版本：
	
	解决数据集预览功能对于包含'.'的属性列无法显示的BUG；
	改进数据集属性表格功能，每次预览都重置自动生成的属性，避免多次修改查询语句预览后导致属性混乱；
	友好提示DataSetException、SqlDataSetSqlExecutionException；
	
待定：
	为数据集添加常用的DataSetResultTransformer功能，比如求和，平均，最大，最小等；
	全局看板资源管理功能；
	图表参数；
	
待研究：
	代理数据集：支持新建“代理数据集”，选定另一个数据集，并可以对其应用DataSetResultTransformer功能，好处是可以复用原始数据集；
	合并数据集：支持新建“合并数据集”，可以根据指定的合并属性，将多个选定的数据集合并为一个数据集；
	看板可视化编辑；
	UI改进：改进现有的UI样式，或者替换为更流行的UI；
	SQL跨库查询；
	docker安装镜像；