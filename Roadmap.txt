下一主版本：
	
	ok 扩展数据集功能，支持Excel、CSV、HTTP API数据集；
	ok 提高SQL数据集的SQL类型兼容性，仅排除明确不支持的类型，其他都作为字符串处理；
	ok 数据集结果处理添加数据格式逻辑，支持用户自定义属性类型；
	ok 地图联动，目标地图有缩放情况时，切换联动，目标地图显示空白；
	ok 改进雷达图图表，支持行式数据结构；
	
下一修订版本：
	
	
	
待定：
	
	数据分析添加项目概念，用于分组管理数据集、图表、看板；
	组合数据集：新建组合数据集（CombineDataSet），选定多个其他数据集，通过SQL语句连接、选取它们，生成新的数据；
	全局看板资源管理功能；
	图表参数；
	
待研究：
	
	应用级集成；
	看板可视化编辑；
	3D图表；
	UI改进：改进现有的UI样式，或者替换为更流行的UI；
	SQL跨库查询；
	docker安装镜像；
