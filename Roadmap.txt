下一主版本：
	
	扩展数据集功能，支持Excel、HTTP API数据集；
	提高SQL数据集的SQL类型兼容性，仅排除明确不支持的类型，其他都作为字符串处理；
	数据集结果处理添加数据格式逻辑，支持用户自定义属性类型；
	地图联动，目标地图有缩放情况时，切换联动，目标地图显示空白；
	SQL Server数据管理功能更新主键字段报错：无法更新标识列；
	权限限制有问题任何一个用户都可操作了，管理员限制匿名用户的权限也是没有用的；
	
下一修订版本：
	
	
	
待定：
	
	组合数据集：新建组合数据集（CombineDataSet），选定多个其他数据集，通过SQL语句连接、选取它们，生成新的数据；
	全局看板资源管理功能；
	图表参数；
	
待研究：
	
	应用级集成；
	看板可视化编辑；
	3D图表；
	UI改进：改进现有的UI样式，或者替换为更流行的UI；
	SQL跨库查询；
	docker安装镜像；
